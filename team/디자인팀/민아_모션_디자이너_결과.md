# LIANCP 쇼케이스 랜딩 — 완전체 (showcase.html 초월 버전)

## 🎯 핵심 전략: "AI가 회사를 만든다" 비주얼 증명

```
Hero: LiquidChrome 3D 히어로 (마우스 반응 크롬 액체)
      ↓ 숫자 폭발 애니메이션 (SplitText + CountUp)
      ↓ Horizontal Scroll 섹션 (5단계 회사 생성 프로세스)
      ↓ 3D 통계 카드 (Tilt + Glow)
      ↓ CTA (Silk 배경 + Magnetic Button)
```

---

## 📦 최종 파일: `liancp-showcase.html`

```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LIANCP — AI가 회사를 만듭니다</title>
  
  <!-- GSAP 3.12.5 고정 -->
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/gsap.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/ScrollTrigger.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/SplitText.min.js"></script>
  
  <!-- Lenis Smooth Scroll -->
  <script src="https://cdn.jsdelivr.net/gh/studio-freight/lenis@1.0.29/bundled/lenis.min.js"></script>
  
  <!-- Three.js (WebGL) -->
  <script src="https://cdn.jsdelivr.net/npm/three@0.160.0/build/three.min.js"></script>
  
  <!-- OGL (경량 WebGL) -->
  <script src="https://cdn.jsdelivr.net/npm/ogl@0.0.102/dist/ogl.umd.js"></script>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      background: #0a0a0a;
      color: #fff;
      font-family: 'Inter', -apple-system, sans-serif;
      overflow-x: hidden;
    }

    /* === Hero Section (LiquidChrome Background) === */
    #hero {
      position: relative;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
    }

    #liquidChrome {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: 1;
    }

    .hero-content {
      position: relative;
      z-index: 10;
      text-align: center;
      max-width: 1200px;
      padding: 0 40px;
    }

    .hero-title {
      font-size: clamp(48px, 8vw, 120px);
      font-weight: 900;
      line-height: 1.1;
      letter-spacing: -0.02em;
      margin-bottom: 40px;
      background: linear-gradient(135deg, #fff 0%, #888 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .hero-subtitle {
      font-size: clamp(18px, 2.5vw, 32px);
      font-weight: 300;
      line-height: 1.6;
      opacity: 0.8;
      margin-bottom: 60px;
    }

    .stats-row {
      display: flex;
      gap: 80px;
      justify-content: center;
      flex-wrap: wrap;
    }

    .stat-item {
      text-align: center;
    }

    .stat-number {
      font-size: clamp(40px, 6vw, 80px);
      font-weight: 900;
      color: #00ff88;
      display: block;
      line-height: 1;
    }

    .stat-label {
      font-size: 16px;
      font-weight: 500;
      opacity: 0.6;
      margin-top: 12px;
      letter-spacing: 0.05em;
      text-transform: uppercase;
    }

    /* === Horizontal Scroll Section === */
    #process {
      position: relative;
      padding: 200px 0;
      background: linear-gradient(180deg, #0a0a0a 0%, #1a1a1a 100%);
    }

    .scroll-container {
      position: relative;
      height: 100vh;
      overflow: hidden;
    }

    .scroll-wrapper {
      display: flex;
      height: 100%;
      will-change: transform;
    }

    .scroll-section {
      min-width: 100vw;
      height: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 0 80px;
      position: relative;
    }

    .section-content {
      max-width: 600px;
    }

    .section-number {
      font-size: 120px;
      font-weight: 900;
      color: rgba(255, 255, 255, 0.05);
      line-height: 1;
      margin-bottom: 20px;
    }

    .section-title {
      font-size: 48px;
      font-weight: 800;
      margin-bottom: 24px;
      background: linear-gradient(135deg, #00ff88 0%, #00aaff 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .section-desc {
      font-size: 20px;
      line-height: 1.8;
      opacity: 0.7;
    }

    /* === 3D Cards Section === */
    #cards {
      padding: 200px 40px;
      background: #0a0a0a;
    }

    .cards-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
      gap: 40px;
      max-width: 1400px;
      margin: 0 auto;
    }

    .card-3d {
      background: linear-gradient(135deg, rgba(255,255,255,0.05) 0%, rgba(255,255,255,0.02) 100%);
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 24px;
      padding: 60px 40px;
      text-align: center;
      position: relative;
      transform-style: preserve-3d;
      transition: transform 0.3s ease;
      cursor: pointer;
    }

    .card-3d:hover {
      border-color: #00ff88;
    }

    .card-3d::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: radial-gradient(circle at 50% 50%, rgba(0,255,136,0.1) 0%, transparent 70%);
      border-radius: 24px;
      opacity: 0;
      transition: opacity 0.3s ease;
    }

    .card-3d:hover::before {
      opacity: 1;
    }

    .card-icon {
      font-size: 64px;
      margin-bottom: 24px;
    }

    .card-title {
      font-size: 28px;
      font-weight: 700;
      margin-bottom: 16px;
    }

    .card-value {
      font-size: 48px;
      font-weight: 900;
      color: #00ff88;
      margin-bottom: 12px;
    }

    .card-label {
      font-size: 14px;
      opacity: 0.6;
      text-transform: uppercase;
      letter-spacing: 0.1em;
    }

    /* === CTA Section (Silk Background) === */
    #cta {
      position: relative;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
    }

    #silkBg {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: 1;
    }

    .cta-content {
      position: relative;
      z-index: 10;
      text-align: center;
      max-width: 800px;
      padding: 0 40px;
    }

    .cta-title {
      font-size: clamp(40px, 6vw, 72px);
      font-weight: 900;
      margin-bottom: 32px;
      line-height: 1.2;
    }

    .cta-desc {
      font-size: 20px;
      line-height: 1.8;
      opacity: 0.8;
      margin-bottom: 60px;
    }

    .magnetic-btn {
      display: inline-block;
      padding: 24px 64px;
      font-size: 20px;
      font-weight: 700;
      color: #0a0a0a;
      background: linear-gradient(135deg, #00ff88 0%, #00aaff 100%);
      border: none;
      border-radius: 50px;
      cursor: pointer;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
      box-shadow: 0 20px 60px rgba(0, 255, 136, 0.3);
    }

    .magnetic-btn:hover {
      transform: scale(1.05);
      box-shadow: 0 30px 80px rgba(0, 255, 136, 0.5);
    }

    /* === Mobile Responsive === */
    @media (max-width: 768px) {
      .stats-row {
        gap: 40px;
      }

      .scroll-section {
        padding: 0 40px;
      }

      .section-number {
        font-size: 80px;
      }

      .section-title {
        font-size: 32px;
      }

      .cards-grid {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>

  <!-- ========== HERO SECTION ========== -->
  <section id="hero">
    <canvas id="liquidChrome"></canvas>
    <div class="hero-content">
      <h1 class="hero-title">AI가 회사를 만듭니다</h1>
      <p class="hero-subtitle">
        사업자등록부터 법인설립까지<br>
        3분 만에 자동 완성되는 기업 창조 시