# 🚀 LIANCP 쇼케이스 랜딩페이지 — 다크 퓨처리스틱 3D 히어로

## 📋 전략 분석

**핵심 목표**: AI 자동 회사 설립 시스템의 파워를 **시각적 충격**으로 전달
- **타겟 임팩트**: showcase.html 대비 120% 이상 비주얼 퀄리티
- **기술 스택**: Vanilla WebGL 2.0 (Three.js 없음) + GSAP + Intersection Observer
- **차별화 포인트**: 
  1. 실제 3D 셰이더 배경 (LiquidChrome 포팅)
  2. 숫자 카운트업 애니메이션 (Odometer.js 스타일)
  3. 스크롤 시네마틱 전환 (GSAP ScrollTrigger)

---

## 🎨 디자인 시스템 (react-bits 기반)

### 1. 히어로 배경: **LiquidChrome** 셰이더 포팅
**소스**: `components/react-bits/src/content/Backgrounds/LiquidChrome/LiquidChrome.jsx`

**원본 코드 분석** (OGL 기반 → Vanilla WebGL 변환):
```javascript
// LiquidChrome.jsx 핵심 (OGL Mesh + frag shader)
import { Renderer, Program, Mesh, Triangle } from 'ogl';

const fragment = `
  precision highp float;
  uniform float uTime;
  uniform vec2 uMouse;
  varying vec2 vUv;
  
  void main() {
    vec2 uv = vUv - 0.5;
    float d = length(uv - uMouse * 0.5);
    float ripple = sin(d * 20.0 - uTime * 3.0) * 0.5 + 0.5;
    vec3 chrome = vec3(0.7 + ripple * 0.3);
    gl_FragColor = vec4(chrome, 1.0);
  }
`;
```

**Vanilla WebGL 2.0 포팅 코드** (자체 구현, 라이브러리 없음):

```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LIANCP — AI Company Auto-Formation System</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { 
      font-family: 'Inter', -apple-system, sans-serif;
      background: #000;
      color: #fff;
      overflow-x: hidden;
    }
    
    /* WebGL 캔버스 (풀스크린 배경) */
    #heroCanvas {
      position: fixed;
      top: 0; left: 0;
      width: 100vw; height: 100vh;
      z-index: 0;
    }
    
    /* 히어로 섹션 */
    .hero {
      position: relative;
      height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      z-index: 1;
    }
    
    .hero h1 {
      font-size: clamp(2.5rem, 8vw, 6rem);
      font-weight: 900;
      letter-spacing: -0.03em;
      background: linear-gradient(135deg, #fff 0%, #888 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-bottom: 1.5rem;
      opacity: 0;
      transform: translateY(30px);
    }
    
    .hero p {
      font-size: clamp(1rem, 2vw, 1.5rem);
      color: #aaa;
      max-width: 600px;
      margin-bottom: 3rem;
      opacity: 0;
      transform: translateY(30px);
    }
    
    /* 숫자 카운터 */
    .stats {
      display: flex;
      gap: 4rem;
      margin-top: 2rem;
    }
    
    .stat {
      text-align: center;
    }
    
    .stat-number {
      font-size: clamp(2rem, 5vw, 4rem);
      font-weight: 700;
      background: linear-gradient(135deg, #00ffff 0%, #0088ff 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      font-variant-numeric: tabular-nums;
    }
    
    .stat-label {
      font-size: 0.875rem;
      color: #666;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      margin-top: 0.5rem;
    }
    
    /* CTA 버튼 */
    .cta {
      display: inline-block;
      padding: 1rem 2.5rem;
      background: linear-gradient(135deg, #00ffff 0%, #0088ff 100%);
      color: #000;
      text-decoration: none;
      font-weight: 700;
      border-radius: 50px;
      transition: transform 0.3s;
      opacity: 0;
      transform: translateY(30px);
    }
    
    .cta:hover {
      transform: translateY(-2px);
      box-shadow: 0 20px 40px rgba(0, 255, 255, 0.3);
    }
    
    /* 스크롤 섹션 */
    .section {
      position: relative;
      min-height: 100vh;
      padding: 8rem 2rem;
      z-index: 1;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    
    .section-content {
      max-width: 1200px;
      width: 100%;
    }
    
    .section h2 {
      font-size: clamp(2rem, 5vw, 3.5rem);
      margin-bottom: 2rem;
      opacity: 0;
      transform: translateX(-50px);
    }
    
    .section p {
      font-size: 1.25rem;
      color: #aaa;
      line-height: 1.8;
      opacity: 0;
      transform: translateX(-50px);
    }
  </style>
</head>
<body>

<!-- WebGL 히어로 배경 -->
<canvas id="heroCanvas"></canvas>

<!-- 히어로 섹션 -->
<section class="hero">
  <h1 id="heroTitle">AI가 회사를 만든다</h1>
  <p id="heroDesc">LIANCP — 자동화된 법인 설립 시스템. 24시간 안에 사업자등록증 발급까지.</p>
  
  <div class="stats">
    <div class="stat">
      <div class="stat-number" data-target="1247">0</div>
      <div class="stat-label">설립 완료</div>
    </div>
    <div class="stat">
      <div class="stat-number" data-target="98">0</div>
      <div class="stat-label">자동화율 (%)</div>
    </div>
    <div class="stat">
      <div class="stat-number" data-target="18">0</div>
      <div class="stat-label">평균 소요 시간 (시간)</div>
    </div>
  </div>
  
  <a href="#start" class="cta" id="ctaBtn">지금 시작하기</a>
</section>

<!-- 스크롤 시네마틱 섹션들 -->
<section class="section" data-section="1">
  <div class="section-content">
    <h2>완전 자동화된 법인 설립</h2>
    <p>서류 작성, 공증, 등기까지 모든 과정을 AI가 처리합니다. 사람이 할 일은 승인 버튼 클릭뿐.</p>
  </div>
</section>

<section class="section" data-section="2">
  <div class="section-content">
    <h2>24시간 이내 사업자등록증</h2>
    <p>기존 2주 → 1일로 단축. 실시간 진행 상황 트래킹과 알림 시스템 제공.</p>
  </div>
</section>

<section class="section" data-section="3">
  <div class="section-content">
    <h2>98% 자동화율, 0% 인적 오류</h2>
    <p>AI가 법령 데이터베이스를 실시간 학습. 서류 누락이나 오타로 인한 반려 사례 제로.</p>
  </div>
</section>

<script>
// ============================================================
// 1. VANILLA WEBGL 2.0 — LIQUID CHROME SHADER (OGL 포팅)
// ============================================================
const canvas = document.getElementById('heroCanvas');
const gl = canvas.getContext('webgl2');

if (!gl) {
  alert('WebGL 2.0을 지원하지 않는 브라우저입니다.');
}

// 캔버스 크기 설정
function resizeCanvas() {
  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;
  gl.viewport(0, 0, canvas.width, canvas.height);
}
resizeCanvas();
window.addEventListener('resize', resizeCanvas);

// 셰이더 컴파일 함수
function createShader(type, source) {
  const shader = gl.createShader(type);
  gl.shaderSource(shader, source);
  gl.compileShader(shader);
  
  if (!gl.getShaderParameter(shader, gl.COMPILE_STATUS)) {
    console.error('셰이더 컴파일 에러:', gl.getShaderInfoLog(shader));
    gl.deleteShader(shader);
    return null;
  }
  return shader;
}

// 프로그램 생성
function createProgram(vertexSource, fragmentSource) {
  const vertexShader = createShader(gl.VERTEX_SHADER, vertexSource);
  const fragmentShader = createShader(gl.FRAGMENT_SHADER, fragmentSource);
  
  const program = gl.createProgram();
  gl.attachShader(program, vertexShader);
  gl.attachShader(program, fragmentShader);
  gl.linkProgram(program);
  
  if (!gl.getProgramParameter(program, gl.LINK_STATUS)) {
    console.error('프로그램 링크 에러:', gl.getProgramInfoLog(program));
    return null;
  }
  