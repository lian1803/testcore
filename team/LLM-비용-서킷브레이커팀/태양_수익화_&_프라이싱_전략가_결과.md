# 🚀 오늘 Vercel 배포 완성 — Mock 제거 & 실제 API 연동 전투 계획

## 📊 현재 상황 진단 (하늘의 검증)

**Mock 잔존 영역 체크리스트**:
- [ ] 회원가입/로그인 (현재 localStorage?)
- [ ] 대시보드 프로젝트 목록 (하드코딩 데이터?)
- [ ] 토큰/비용 집계 (프론트 계산?)
- [ ] Slack/이메일 알림 (console.log?)

**오늘 배포 가능 기준**: Supabase Auth + 프로젝트 CRUD + 실시간 토큰 집계 API 3개 완성 = Vercel 배포 조건 충족

---

## 🔧 1단계: Supabase 실제 연동 (서연 + 민재, 60분)

### 1-1. Supabase 프로젝트 설정

```bash
# .env.local 필수 변수 (Supabase Dashboard에서 복사)
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc... # 백엔드 전용
```

**테이블 스키마 (SQL Editor에 붙여넣기)**:
```sql
-- 회원 프로필 (Auth 테이블과 1:1)
CREATE TABLE profiles (
  id UUID REFERENCES auth.users PRIMARY KEY,
  email TEXT UNIQUE NOT NULL,
  full_name TEXT,
  tier TEXT DEFAULT 'free' CHECK (tier IN ('free', 'pro', 'team')),
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 프로젝트 (1회원 N프로젝트)
CREATE TABLE projects (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES profiles(id) ON DELETE CASCADE,
  name TEXT NOT NULL,
  api_key TEXT UNIQUE NOT NULL, -- SDK 인증용
  budget_limit INTEGER DEFAULT 10000, -- 센트 단위
  current_spend INTEGER DEFAULT 0,
  status TEXT DEFAULT 'active' CHECK (status IN ('active', 'paused', 'over_budget')),
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 토큰 사용 로그 (실시간 집계용)
CREATE TABLE token_logs (
  id BIGSERIAL PRIMARY KEY,
  project_id UUID REFERENCES projects(id) ON DELETE CASCADE,
  model TEXT NOT NULL, -- 'gpt-4', 'claude-3', etc
  input_tokens INTEGER DEFAULT 0,
  output_tokens INTEGER DEFAULT 0,
  cost_usd NUMERIC(10,6), -- 달러 6자리 소수점
  timestamp TIMESTAMPTZ DEFAULT NOW()
);

-- 인덱스 (쿼리 속도 100배 향상)
CREATE INDEX idx_token_logs_project_time ON token_logs(project_id, timestamp DESC);
CREATE INDEX idx_projects_user ON projects(user_id);
```

### 1-2. 회원가입 실제 구현 (준혁 코드 리뷰 + 민재 적용)

**Before (Mock)**:
```typescript
// ❌ 기존 Mock 코드 (삭제 대상)
const handleSignup = async (email: string, password: string) => {
  localStorage.setItem('user', JSON.stringify({ email }));
  router.push('/dashboard');
};
```

**After (Supabase Auth)**:
```typescript
// ✅ app/signup/page.tsx
'use client';
import { createClientComponentClient } from '@supabase/auth-helpers-nextjs';
import { useState } from 'react';
import { useRouter } from 'next/navigation';

export default function SignupPage() {
  const supabase = createClientComponentClient();
  const router = useRouter();
  const [loading, setLoading] = useState(false);

  const handleSignup = async (e: React.FormEvent<HTMLFormElement>) => {
    e.preventDefault();
    setLoading(true);

    const formData = new FormData(e.currentTarget);
    const email = formData.get('email') as string;
    const password = formData.get('password') as string;

    try {
      // 1. Auth 회원가입
      const { data: authData, error: authError } = await supabase.auth.signUp({
        email,
        password,
        options: {
          emailRedirectTo: `${location.origin}/auth/callback`, // 이메일 인증 후 리다이렉트
        }
      });

      if (authError) throw authError;

      // 2. Profile 생성 (Trigger로 자동화 가능하지만 명시적 처리)
      const { error: profileError } = await supabase
        .from('profiles')
        .insert({ id: authData.user!.id, email, tier: 'free' });

      if (profileError) throw profileError;

      alert('이메일 인증 링크를 확인하세요!');
      router.push('/login');
    } catch (error: any) {
      alert(error.message);
    } finally {
      setLoading(false);
    }
  };

  return (
    <form onSubmit={handleSignup} className="max-w-md mx-auto p-6">
      <input name="email" type="email" required placeholder="Email" className="border p-2 w-full mb-4" />
      <input name="password" type="password" required placeholder="Password (6자 이상)" className="border p-2 w-full mb-4" />
      <button disabled={loading} className="bg-blue-600 text-white px-6 py-2 rounded">
        {loading ? '처리 중...' : '가입하기'}
      </button>
    </form>
  );
}
```

**이메일 인증 콜백 처리**:
```typescript
// app/auth/callback/route.ts
import { createRouteHandlerClient } from '@supabase/auth-helpers-nextjs';
import { cookies } from 'next/headers';
import { NextResponse } from 'next/server';

export async function GET(request: Request) {
  const requestUrl = new URL(request.url);
  const code = requestUrl.searchParams.get('code');

  if (code) {
    const supabase = createRouteHandlerClient({ cookies });
    await supabase.auth.exchangeCodeForSession(code);
  }

  return NextResponse.redirect(new URL('/dashboard', requestUrl.origin));
}
```

---

## 🎯 2단계: 대시보드 프로젝트 CRUD (서연 + 민재, 90분)

### 2-1. 프로젝트 목록 불러오기

**Before (Mock)**:
```typescript
// ❌ 삭제
const projects = [
  { id: '1', name: 'Mock Project', spend: 45.23 }
];
```

**After (실시간 Supabase 쿼리)**:
```typescript
// app/dashboard/page.tsx
'use client';
import { createClientComponentClient } from '@supabase/auth-helpers-nextjs';
import { useEffect, useState } from 'react';

export default function DashboardPage() {
  const supabase = createClientComponentClient();
  const [projects, setProjects] = useState<any[]>([]);
  const [user, setUser] = useState<any>(null);

  useEffect(() => {
    // 1. 현재 로그인 유저 가져오기
    supabase.auth.getUser().then(({ data }) => {
      setUser(data.user);
      
      // 2. 해당 유저의 프로젝트 목록 조회
      if (data.user) {
        supabase
          .from('projects')
          .select('*')
          .eq('user_id', data.user.id)
          .order('created_at', { ascending: false })
          .then(({ data: projects }) => setProjects(projects || []));
      }
    });

    // 3. 실시간 구독 (프로젝트 추가/삭제 시 자동 업데이트)
    const channel = supabase
      .channel('projects-changes')
      .on('postgres_changes', 
        { event: '*', schema: 'public', table: 'projects' },
        () => {
          // 변경 감지 시 다시 조회
          supabase.auth.getUser().then(({ data }) => {
            if (data.user) {
              supabase.from('projects').select('*').eq('user_id', data.user.id)
                .then(({ data }) => setProjects(data || []));
            }
          });
        }
      )
      .subscribe();

    return () => { supabase.removeChannel(channel); };
  }, []);

  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold mb-4">내 프로젝트 ({projects.length})</h1>
      <div className="grid gap-4">
        {projects.map(p => (
          <div key={p.id} className="border p-4 rounded">
            <h3 className="font-bold">{p.name}</h3>
            <p className="text-sm text-gray-600">예산: ${(p.budget_limit/100).toFixed(2)} | 사용: ${(p.current_spend/100).toFixed(2)}</p>
            <p className={`text-xs ${p.status === 'over_budget' ? 'text-red-600' : 'text-green-600'}`}>
              상태: {p.status}
            </p>
          </div>
        ))}
      </div>
    </div>
  );
}
```

### 2-2. 프로젝트 생성 (API Key 자동 발급)

```typescript
// app/api/projects/route.ts
import { createRouteHandlerClient } from '@supabase/auth-helpers-nextjs';
import { cookies } from 'next/headers';
import { NextResponse } from 'next/server';
import crypto from 'crypto';

export async function POST(request: Request) {
  const supabase = createRouteHandlerClient({ cookies });
  const { data: { user } } = await supabase.auth.getUser();

  if (!user) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }

  const { name, budget_limit } = await request.json();

  // API Key 생성 (llm-cb-로 시작, 32자 랜덤)
  const apiKey = `llm-cb-${crypto.randomBytes(16).toString('hex')}`;

  const { data, error } = await supabase
    .from('projects')
    .insert({
      user_id: user.id,
      name,
      budget_limit: budget_limit * 100, // 달러 → 센트
      api_key: apiKey,
    })
    .select()
    .single();

  if (